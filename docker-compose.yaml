# agentco, self-hosted. -> docs/SPEC-deploy.md §3.1
#
#   docker compose up -d        then open http://127.0.0.1:7317
#
# ┌──────────────────────────────────────────────────────────────────────────
# │ WHAT THIS FILE DOES NOT DO, AND WHY THAT IS THE POINT.
# │
# │ No TLS, no reverse proxy, no domain, no user accounts. The perimeter is the
# │ operator's to design, because only the operator knows whether this sits
# │ behind Cloudflare, inside a company VPN, on a Tailscale network, or on a
# │ laptop. Shipping a guessed nginx config would be shipping a lie about an
# │ infrastructure we cannot see. Every self-hosted product draws the line in
# │ the same place.
# │
# │ 🔴 WHAT THE OPERATOR NEEDS TO KNOW BEFORE MOVING THAT LINE:
# │ agentco authenticates with ONE SHARED TOKEN. There are no user accounts.
# │ Anyone holding the token is, to this daemon, the owner: no per-person
# │ revocation, no record of who did what, and rotating it logs everyone out at
# │ once. That is a fact to design around, not a bug to wait on — put an
# │ identity layer IN FRONT (Cloudflare Access, oauth2-proxy, Tailscale) and
# │ the shared token goes back to being an internal detail between the proxy
# │ and the daemon, which is the right job for it.
# └──────────────────────────────────────────────────────────────────────────

services:
  agentco:
    build:
      context: .
      dockerfile: docker/Dockerfile
      # Pin a version instead of tracking latest:
      #   args: { AGENTCO_VERSION: "0.1.6" }

    # 🔴 127.0.0.1 ON PURPOSE — the port is reachable from THIS MACHINE ONLY,
    # including when this machine is a VPS. Dropping the prefix publishes the
    # daemon to every interface, and on most hosts that means the internet.
    #
    # Reaching it from elsewhere WITHOUT publishing it:
    #   ssh -L 7317:127.0.0.1:7317 user@your-vps
    # and the browser still opens 127.0.0.1:7317. That route also keeps Google
    # OAuth on agentco's own app — loopback is exempt from Google's HTTPS rule,
    # a real domain is not. -> SPEC-deploy §2 · §4
    #
    # ⚠ ONE VARIABLE, BECAUSE TWO COPIES OF A PORT NUMBER DRIFT. It appears here
    # and in AGENTCO_RUNTIME_PUBLIC_URL below, and those two disagreeing is a
    # `redirect_uri_mismatch` whose message never mentions a port. Change it in
    # a `.env` beside this file: AGENTCO_PORT=7318
    #
    # ⚠ AGENTCO'S OWN PORT SELF-HEALING CANNOT HELP HERE. When 7317 is already
    # taken on the host — by a desktop install of agentco, most likely — Docker
    # fails to publish the port and the container never starts, so the daemon
    # that knows how to move is never asked. Measured 17/09: this is exactly
    # what happens when you already run agentco on the same machine.
    # ⚠ THE SAME NUMBER ON BOTH SIDES, on purpose. Docker's usual shape is
    # `host:container` with a fixed container port, and `7319->7317` in
    # `docker ps` then asks the reader to translate every time. One number is
    # one fewer thing to wonder about — the same instinct as
    # `docs/CLAUDE.md §ONE NUMBER`.
    ports:
      - "127.0.0.1:${AGENTCO_PORT:-7317}:${AGENTCO_PORT:-7317}"

    environment:
      # ┌──────────────────────────────────────────────────────────────────────
      # │ 🔴 WITHOUT THIS LINE, EVERY OAUTH LOGIN REFUSES TO START.
      # │
      # │ `redirectBase` has three branches, and a container lands in the third:
      # │ bound to 0.0.0.0, nothing declared -> REFUSE. That is deliberate — the
      # │ redirect is where the authorization code is delivered, and `Host` is
      # │ sent by the client, so guessing it would let a caller choose where the
      # │ code lands. -> src/server/oauth-routes.ts
      # │
      # │ Declaring loopback here is accepted even though the daemon binds
      # │ 0.0.0.0, because the address it hands the provider is still loopback —
      # │ which is also what keeps Google on agentco's own app, no keys, no
      # │ Cloud Console. -> SPEC-deploy §2 · §4
      # │
      # │ ⚠ IT MUST MATCH THE PORT ON THE LEFT OF `ports:` ABOVE — the address
      # │ the BROWSER uses, not the one inside the container. Remap to
      # │ "127.0.0.1:8080:7317" and this becomes http://127.0.0.1:8080. A
      # │ mismatch produces `redirect_uri_mismatch`, whose message never
      # │ mentions the port.
      # └──────────────────────────────────────────────────────────────────────
      # ⚠ THE CONFIG VALUE, NOT A `--port` FLAG. `cli/index.ts §433` writes a
      # moved port back to company.yaml only when it came from the config —
      # "`--port` is for this run and must not edit the file". Passing the flag
      # would leave company.yaml claiming 7317 while the daemon answers
      # elsewhere, which is exactly the drift the port self-healing was built to
      # prevent. `AGENTCO_RUNTIME_PORT` sets the config itself, through the same
      # env-over-file mechanism as the line below. -> core/config.ts
      AGENTCO_RUNTIME_PORT: "${AGENTCO_PORT:-7317}"

      # ⚠ OVERRIDABLE FROM `.env`, with the loopback address as the fallback.
      # It used to be hardcoded here, which meant the one case that REQUIRES a
      # different value — a domain in front, or a mapping whose outside port
      # differs from the inside one — could only be fixed by editing this file.
      # Asking someone to edit the compose file to deploy it is asking them to
      # maintain a fork of it. → `.env.example §The public address`
      AGENTCO_RUNTIME_PUBLIC_URL: "${AGENTCO_RUNTIME_PUBLIC_URL:-http://127.0.0.1:${AGENTCO_PORT:-7317}}"

      # ┌──────────────────────────────────────────────────────────────────────
      # │ SIGNING IN TO CLAUDE — MINTED BEFORE `up`, NOT INSIDE THE CONTAINER.
      # │
      # │ A subscription is the usual choice, and `claude setup-token` hands you
      # │ a long-lived token that is consumed as an ENVIRONMENT VARIABLE:
      # │
      # │     Use this token by setting: export CLAUDE_CODE_OAUTH_TOKEN=<token>
      # │
      # │ So it has the same shape as an API key — one string — and it belongs
      # │ in a `.env` beside this file. Run `agentco login --token` on any
      # │ machine (your own is fine — it brings its own Claude Code, so one does
      # │ not have to be installed there), paste the result,
      # │ and `docker compose up -d` is genuinely the only command: the
      # │ container is ready to chat on its first start.
      # │
      # │ ⚠ Minting it INSIDE the container also works but needs a REAL
      # │ terminal — `docker compose exec -it` cannot allocate one from a
      # │ non-interactive shell, and `setup-token` is a raw-mode prompt that
      # │ then waits forever, printing nothing. Measured 17/09, the hard way.
      # │
      # │ ⚠ Set ONE of these two. Both together is not an error, but it makes
      # │ "which credential is this bill on" a question nobody can answer by
      # │ reading the file.
      # └──────────────────────────────────────────────────────────────────────
      CLAUDE_CODE_OAUTH_TOKEN: ${CLAUDE_CODE_OAUTH_TOKEN:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}

      # ⚠ OFF, BECAUSE IN HERE IT WOULD LIE. The update button runs
      # `npm install -g`, which writes to the container's own filesystem — not
      # to this volume. It would appear to work, and then vanish the next time
      # the container is recreated. The update path for a container is the
      # image: `docker compose pull && docker compose up -d` (or rebuild).
      AGENTCO_UPDATES_CHECK: "false"

    # ┌──────────────────────────────────────────────────────────────────────
    # │ TWO MOUNTS, BECAUSE ONLY ONE OF THEM IS YOURS TO LOOK AT.
    # │
    # │ The company is a real folder on your disk: you open it, edit it, diff
    # │ it, back it up by copying it, and carry it to another machine the same
    # │ way. That is the point of the product — the value lives in the files,
    # │ and files you cannot see are files you do not own.
    # │
    # │ `/data/home` stays a named volume on purpose. It is `~/.claude`, and
    # │ `projects/` under it grows a conversation record for every session —
    # │ large, constant, and read by nobody with their eyes. Emptying that into
    # │ your project folder would bury the part you actually came for.
    # │
    # │ ⚠ THREE COSTS OF THE BIND MOUNT, stated rather than discovered:
    # │   · On a Linux host the container runs as root, so files it creates are
    # │     root-owned and need `sudo` to edit. Docker Desktop hides this.
    # │   · Bind mounts on Docker Desktop cross a VM filesystem boundary; a
    # │     folder with many small reads and writes can feel slower than the
    # │     named volume did.
    # │   · The agent will name paths as `/data/company/…` while you know them
    # │     as `<this folder>/company/…`. Harmless — `officeJail` fences on
    # │     resolved paths inside the container — but it reads oddly.
    # │
    # │ ⚠ `COMPANY_PATH`, deliberately WITHOUT the `AGENTCO_` prefix: in this
    # │ project that prefix means "an app config override" (core/config.ts maps
    # │ `AGENTCO_RUNTIME_PORT` onto `runtime.port`). This is a host path that
    # │ compose reads and the app never sees, so borrowing the prefix would be
    # │ borrowing a meaning that is already taken.
    # └──────────────────────────────────────────────────────────────────────
    volumes:
      # ⚠ The fallback must be the same path `.env.example` offers. Somebody who
      # never made a `.env` would otherwise land in `./company` — the folder
      # `agentco init` uses — and two daemons would share one company without
      # anyone choosing that. A test ties the two strings together.
      - ${COMPANY_PATH:-./docker/company}:/data/company
      - agentco-home:/data/home

    # ⚠ EXACTLY ONE REPLICA, and this is not a formality: the OAuth `state` ↔
    # verifier pair lives in RAM (SPEC-deploy §5③). A second instance behind a
    # load balancer would fail logins at exactly the load-splitting ratio.
    # For the same reason, avoid restarting while someone is mid-login.
    restart: unless-stopped

    # `/healthz` is outside the token gate and reports the daemon's own version,
    # so it answers honestly even before anyone has logged in. -> server.ts
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - "fetch('http://127.0.0.1:${AGENTCO_PORT:-7317}/healthz').then(r => process.exit(r.ok ? 0 : 1), () => process.exit(1))"
      interval: 30s
      timeout: 5s
      start_period: 40s
      retries: 3

volumes:
  # Only Claude Code's own state. The company is a folder on your disk, above.
  agentco-home:
