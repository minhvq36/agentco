node_modules/
dist/
build/
*.tsbuildinfo

web/dist/

/tmp-*/

company/.state/
**/.state*/
**/secrets.json
*.session.jsonl

/company/
# The Docker door's company. It lives under docker/ so it can never collide with
# the desktop one, and it holds .state/secrets.json — live tokens for every
# account the person has connected.
/docker/company/
/.bench-company/

logs/
*.log
bench/baseline.json
bench/results/

.env
.env.*
# ...but the template MUST ship: it is the only place that says a token has to be
# pasted in by hand, and the person who needs it has no other way to find out.
!.env.example
*.pem
*.key

.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

SESSIONS_MEMORY.md
product-decisions-*.md
BUSINESS.md
NOTES.md
notes/

.playwright-mcp/

/*-snapshot.md
/*-console.md

# scripts/package.ts
#
# WARNING: building here makes the smoke test LIE. `out/` sits inside the repo,
# so node module resolution walks UP out of the packaged tree and finds the
# repo's own node_modules — the first run reported Claude Code at
# `agentco/node_modules/...` when the package deliberately contains no such
# thing. Build with `--out <somewhere outside the repo>` to test for real.
/out/
