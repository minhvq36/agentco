# deps / build
node_modules/
dist/
build/
*.tsbuildinfo

# giao diện: mã nguồn trong web/src được commit, bản build thì không.
# `dist/` ở trên đã bắt cả web/dist — ghi rõ ra để khỏi ai tưởng là sót.
web/dist/

# thư mục thử nghiệm tạm khi kiểm bằng tay
/tmp-*/

# state của công ty — KHÔNG BAO GIỜ commit.
# `.state*` chứ không phải `.state`: luật cũ khớp ĐÚNG tên `.state`, nên thư mục
# nháp `.state-spike/` (chìa OAuth thật của spike Notion) nằm ngoài lưới suốt
# ngày 25/08/2026. Mọi biến thể `.state-…` từ giờ đều bị chặn.
company/.state/
**/.state*/
**/secrets.json
*.session.jsonl

# công ty thật của người phát triển (tạo bởi `agentco init` ở gốc repo).
# templates/company/ mới là bản mẫu được commit.
/company/
/.bench-company/

# log & đo đạc
logs/
*.log
bench/baseline.json
bench/results/

# bí mật
.env
.env.*
!.env.example
*.pem
*.key

# hệ điều hành / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# ghi chú cá nhân — tuyệt đối không lọt sang repo này
SESSIONS_MEMORY.md
product-decisions-*.md
BUSINESS.md
NOTES.md
notes/
